<UserControl x:Class="Styling.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="239*" />
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
            <Button x:Name="btStyles" Margin="2" Click="btStyles_Click">Styles</Button>
            <Button x:Name="btBehavior" Margin="2" Click="btBehavior_Click">Behavior</Button>
            <Button x:Name="btControlTemplate" Margin="2" Click="btControlTemplate_Click">Control template</Button>
        </StackPanel>
        <Grid Name="theContainer" Grid.Row="1">

        </Grid>
    </Grid>
</UserControl>
